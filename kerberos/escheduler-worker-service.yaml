workerVersion: v1
kind: Service
metadata:
  labels:
    name: %PREFIX%-escheduler-worker
    app: escheduler-worker
  name: %PREFIX%-escheduler-worker
  namespace: %NAMESPACE%
spec:
  type: ClusterIP
  ports:
    - name: escheduler-worker
      port: 50051
      targetPort: 50051
      protocol: TCP
  selector:
    name: pod-escheduler-worker
    app: escheduler-worker
