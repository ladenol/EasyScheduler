uiVersion: v1
kind: Service
metadata:
  labels:
    name: %PREFIX%-escheduler-ui
    app: escheduler-ui
  name: %PREFIX%-escheduler-ui
  namespace: %NAMESPACE%
spec:
  type: NodePort
  ports:
    - name: escheduler-ui
      port: 8888
      targetPort: 8888
      protocol: TCP
  selector:
    name: pod-escheduler-ui
    app: escheduler-ui
